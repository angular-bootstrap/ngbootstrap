import { InjectionToken } from '@angular/core';

/** Injection token for the active `ngb-datagrid` instance (layout toolbar tools). */
export const NGB_DATAGRID_HOST = new InjectionToken('NgbDatagridHost');
